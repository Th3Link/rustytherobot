[package]
name = "rustytherobot"
version = "0.1.0"
edition = "2024"

[dependencies]
argh = { version = "0.1" }
tracing = {version = "0.1" }
tracing-subscriber = {version = "0.3", features = ["env-filter"] }
